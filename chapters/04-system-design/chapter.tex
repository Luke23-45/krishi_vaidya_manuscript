\chapter{SYSTEM DESIGN}

\ChapterSection{Design}

Four components move the system from the requirements in Chapter~3 to a deployable diagnosis
pipeline: a YOLO model for the plant-part localization, a fine-tuned VLM for disease
classification, a backend that manages inference and advisory generation, and a mobile
application that delivers results to the farmer. The sections below describe each component with its
datasets and configuration, followed by the algorithmic procedures that run across them.

\ChapterSubsection{YOLO Object-Detection Dataset}

Leaves, panicles, grain clusters, fruits, ears, and vegetable heads --- the YOLO component
localizes all of them within crop images. Disease classification belongs to the VLM. The
detector's role is to tell the downstream pipeline which plant parts are
present and where they sit in the frame.

\begin{figure}[htbp]
\centering
\includegraphics[width=1\textwidth,height=0.5\textheight,keepaspectratio]{data/flow_digram_data/build/yolo/06_system_architecture_overview.pdf}
\caption{YOLO System Architecture Overview}
\label{fig:yolo-system-architecture-overview}
\end{figure}
We chose YOLOv8 nano specifically because it is small enough for mobile TFLite export while
reaching usable mAP on the eleven-class taxonomy
\parencite{jiangYOLOSurvey2022,ultralyticsYolov8}.
Figure~\ref{fig:yolo-system-architecture-overview} presents the detection pipeline.

\subsubsection{Data Sources and Class Mapping}

We have collected different datasets of plant classes such as tomato, rice, potato, maize, brassica, cauliflower, and cabbage imagery from the resources Kaggle and Roboflow. 
We have merged the different sources of plant datasets into a single final dataset. In total, there were 34,992 images; out of them, we removed 4,011 images and 13,213 annotations because of the incorrect out-of-bounds coordinates. 
Since we have different classes of plants from multiple sources, there was an imbalance in classes, as rice panicle has 115,046 of 147,467 annotations. But the maize ear has only th 106, and rice grain clusters have only 473. Figure~\ref{fig:yolo-data-pipeline-flow} presents the data distribution based on the different classes. 

\begin{figure}[htbp]
\centering
\includegraphics[width=1\textwidth,height=0.5\textheight,keepaspectratio]{data/flow_digram_data/build/yolo/01_data_pipeline_flow.pdf}
\caption{YOLO Data Pipeline Flow}
\label{fig:yolo-data-pipeline-flow}
\end{figure}

\subsubsection{Dataset Construction}

Our final dataset for the YOLO  training has 11 classes with 30.981 images and 147,467 annotations. For training and validation, the dataset was split into 27,454 images for training and 3,527 for validation. COCO-style pixel \texttt{xywh} format was used to store the annotations or labels. During the training of the YOLO model, the stored COCO-style pixels were converted into the YOLO format. The number of images per class is shown in the table Table~\ref{tab:yolo-dataset-class-distribution}.

{\small
\renewcommand{\arraystretch}{1.3}
\begin{xltabular}{\textwidth}{@{}lr@{}}
\caption{YOLO dataset class distribution after curation}\label{tab:yolo-dataset-class-distribution}\\
\toprule
\textbf{Class} & \textbf{Object annotations} \\
\midrule
\endfirsthead
\multicolumn{2}{l}{\footnotesize\textit{Table \thetable{} \textemdash{} continued from previous page}}\\[3pt]
\toprule
\textbf{Class} & \textbf{Object annotations} \\
\midrule
\endhead
\endfoot
\bottomrule
\endlastfoot
Rice leaf & 1,657 \\
Rice panicle & 115,046 \\
Rice grain cluster & 473 \\
Maize leaf & 8,000 \\
Maize ear & 106 \\
Potato leaf & 1,199 \\
Tomato leaf & 8,792 \\
Tomato fruit & 6,808 \\
Brassica leaf & 3,834 \\
Cauliflower head & 525 \\
Cabbage head & 1,027 \\
\end{xltabular}
}

\ChapterSubsection{VLM Disease-Diagnosis Dataset}

The VLM model Qwen2.5-VL-3B  was fine-tuned for disease classification \parencite{bai2025qwen25vl}. It would take the image of the crop or plan and instructions and process it. After process it would create a JSON diagnosis which would contain the disease, confidence, and other information. As we have used the 4-bit LoRA fine-tuning, it has 3 billion parameters. It could run on a single L4 Nvidia GPU. Figure~\ref{fig:vlm-system-architecture-overview}
shows the architecture for this component.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.9\textwidth,height=0.45\textheight,keepaspectratio]{data/flow_digram_data/build/vlm/06_system_architecture_overview.pdf}
\caption{VLM system architecture overview separating dataset processing, training, inference, and serving infrastructure.}
\label{fig:vlm-system-architecture-overview}
\end{figure}

\subsubsection{Taxonomy and Label Normalization}

Our taxonomy covers rice, maize, potato, tomato, cauliflower, and cabbage across eleven
disease classes: maize common rust, maize healthy, maize northern leaf blight, potato early
blight, potato healthy, potato late blight, tomato early blight, tomato healthy, tomato late
blight, tomato leaf mold, and tomato septoria leaf spot. Canonical labels follow the format
\texttt{crop\_\_disease} --- for example, \texttt{potato\_\_late\_blight}. Since evaluation
relies on exact canonical-label matching, alias normalization at export is a correctness
requirement, not a cosmetic one.

\subsubsection{Dataset Pipeline}

We configured the dataset pipeline under \texttt{vlm\_model/data/configs} as project
\texttt{krishivaidya}, dataset \texttt{kv-core5-ms}. During the dataset split into training, validation and test, the test split contains 15\% which would later be used for testing and 15\% for validation, which would be used during validation during the training phase. Per-crop record caps prevent any single crop from dominating
training: 800 for rice, maize, potato, and tomato; 600 for cauliflower and cabbage
(Figure~\ref{fig:vlm-data-pipeline-flow}).

\begin{figure}[htbp]
\centering
\includegraphics[width=1\textwidth,height=0.6\textheight,keepaspectratio]{data/flow_digram_data/build/vlm/01_data_pipeline_flow.pdf}
\caption{VLM data pipeline flow from raw sources through curation, canonicalization, export, and training data assembly.}
\label{fig:vlm-data-pipeline-flow}
\end{figure}

\subsubsection{Qwen2.5-VL SFT Export}

Each exported record pairs contains an image path with a human message that includes the image token and
instruction, an assistant answer, and metadata fields such as split, crop, disease, canonical
label, template id, and source. The assistant target takes one of three shapes:
\texttt{disease\_json} requests compact JSON with \texttt{crop}, \texttt{disease},
\texttt{canonical\_label}, and \texttt{confidence\_label}; \texttt{label\_only} asks for
just the canonical and confidence labels; \texttt{diagnostic\_short} adds a brief
visual-diagnosis sentence.

LoRA adapters target the attention and feed-forward projection layers, excluding visual
modules. Fine-tuning configuration and prediction parsing follow in Algorithm Details.

\ChapterSubsection{Backend System Architecture}

A TypeScript and Express API lie between the mobile client and every backend concern: farm
records, image storage, authentication, VLM inference, and advisory generation, all behind a
single versioned interface. Configuration, middleware, routing, controllers, services,
repositories, and data models each live in separate layers so that HTTP transport, business
rules, database persistence, and ML integrations do not leak into each other
\parencite{sculleyMLTechDebt2015}.


\begin{figure}[htbp]
\centering
\includegraphics[width=0.9\textwidth,height=0.45\textheight,keepaspectratio]{data/flow_digram_data/build/backend/05_system_architecture_overview.pdf}
\caption{Backend system architecture overview showing the request stack and external service boundaries.}
\label{fig:backend-architecture}
\end{figure}

\subsubsection{Runtime Modes and Application Initialization}

Three runtime modes control which route groups load, set through the \texttt{BACKEND\_MODE}
environment variable: \texttt{diagnose} (model-serving routes only), \texttt{non\_ai} (database
routes only), and \texttt{complete} (all routes). A health endpoint at \texttt{/api/v1/health}
reports server state, uptime, environment, mode, and public base URL. Before the server
starts, Zod validates every configuration variable, preventing silent startup in an incomplete
state. Database-backed routes need JWT, Cloudinary, and SMTP credentials; the diagnosis stack
needs the ML service URL and its authentication configuration.

We wired all dependencies in \texttt{src/container.ts} --- repositories first, then services,
controllers, and route modules --- keeping the dependency graph in one place instead of
scattered across route files.

\ChapterSubsection{Backend API Design}

\subsubsection{Route Groups}

Table~\ref{tab:backend-api-domains-design} lists the implemented groups and their responsibilities.
All routes sit under \texttt{/api/v1}. In development, Swagger UI at \texttt{/api/docs}
exposes request schemas, security requirements, and response documentation generated from
route-level comments.
{\small
\renewcommand{\arraystretch}{1.3}
\begin{xltabular}{\textwidth}{@{} l l L @{}}
\caption{Backend API domain routes}\label{tab:backend-api-domains-design}\\
\toprule
\textbf{Domain} & \textbf{Route} & \textbf{Description} \\
\midrule
\endfirsthead
\multicolumn{3}{l}{\footnotesize\textit{Table \thetable{} \textemdash{} continued from previous page}}\\[3pt]
\toprule
\textbf{Domain} & \textbf{Route} & \textbf{Description} \\
\midrule
\endhead
\midrule
\multicolumn{3}{r}{\textit{(continues on next page)}} \\
\endfoot
\bottomrule
\endlastfoot
Health & \texttt{/api/v1/health} & Status of server, runtime mode, uptime information and exposure of the server to the world \\
Diagnosis & \texttt{/api/v1/diagnose} & Image validation, request a VLM inference. Generation of advice, diagnosis message \\
Authentication & \texttt{/api/v1/auth} & Registration of user, login, refreshing token, OTP and reseting password for the user \\
Users & \texttt{/api/v1/users} & Request profile, update profile, image of the profile for the user, changing password and push tokens for the user \\
Scans & \texttt{/api/v1/scans} & Uploading scans by user, history, statistics, grouping of scans, status of scans, resolution and follow up linkage for scans uploaded by the user \\
Crops & \texttt{/api/v1/crops} & Create \& list plots, filter plots, plots archive, growth-state, health-status \\
Tasks & \texttt{/api/v1/tasks} & Create, edit \& list plot-tasks, create-tasks in batch, pending/overdue/upcoming tasks, \& list tasks \& based on completion state \\
Notes & \texttt{/api/v1/notes} & Create, list plot-notes, linked scan-notes, note-images and number of notes based on note type. \\
Advisories & \texttt{/api/v1/advisories} & Create \& list advisories, filter advisories, list active \& dismiss/undismiss advisories \\
\end{xltabular}
}

\subsubsection{Controller and Validation Process}

Every controller receives the typed service dependencies and exposes Express request handlers. For
validation, we use \texttt{express-validator} to check MongoDB identifiers, bound pagination
values, enumerate types for task categories and advisory severities, and limit text-field
lengths. A shared response formatter sends back successful responses; a global error
middleware catches operational failures.

Most database-backed routes require authentication. On every protected route, the auth
middleware verifies a Bearer token against the configured JWT secret, rejects expired or
malformed tokens, blocks pending-verification and suspended accounts, and attaches the decoded
user context to the request. Service methods then enforce ownership through repository
queries, so access control does not repeat in every controller.

\ChapterSubsection{Backend Data Model}

\subsubsection{MongoDB Collections and Domain Entities}

Mongoose models have nine domain entities: users, OTP records, refresh tokens, scans, scan
resolutions, crop plots, crop tasks, crop notes, and advisories
(Table~\ref{tab:backend-data-models}). 

{\small
\renewcommand{\arraystretch}{1.3}
\begin{xltabular}{\textwidth}{@{} l L L @{}}
\caption{Implemented backend data models}\label{tab:backend-data-models}\\
\toprule
\textbf{Model} & \textbf{Primary purpose} & \textbf{Important implementation details} \\
\midrule
\endfirsthead
\multicolumn{3}{l}{\footnotesize\textit{Table \thetable{} \textemdash{} continued from previous page}}\\[3pt]
\toprule
\textbf{Model} & \textbf{Primary purpose} & \textbf{Important implementation details} \\
\midrule
\endhead
\endfoot
\bottomrule
\endlastfoot
\texttt{User} & User account and preferences & Email, password hash, name, language, region, accessibility, location, status, push tokens \\
\texttt{Otp} & OTP verification state & Purpose-specific OTP hash, attempt count, expiry, unique email-purpose index, TTL expiry \\
\texttt{RefreshToken} & Session refresh state & Token hash, lookup ID, device context, revocation state, expiry, TTL cleanup \\
\texttt{Scan} & Disease scan history & Image URI, crop metadata, status, result object, client timestamp, offline flag, soft delete \\
\texttt{ScanResolution} & Outcome tracking for scans & Resolution type, treatment used, notes, follow-up scan references \\
\texttt{CropPlot} & Farmer crop plot record & Crop type, variety, region, district, area, planting date, stage, health status, archive state \\
\texttt{CropTask} & Farm task and reminder record & Plot link, category, due date, priority, completion state, auto-generated flag \\
\texttt{CropNote} & Crop observation and treatment note & Plot link, optional scan link, note type, title/body, image attachments, soft delete \\
\texttt{Advisory} & Crop advisory notification & Plot link, advisory type, severity, Nepali/English fields, dismiss state, expiry TTL \\
\end{xltabular}
}

\subsubsection{Repository Layer}

Repositories encapsulate database access for user-owned resources --- scans, crop plots,
tasks, notes, and advisories. Each method accepts a user or plot identifier and applies
ownership filters before returning results. Figure~\ref{fig:backend-data-model-persistence-flow} shows the access control inside the data layer.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.9\textwidth,height=0.40\textheight,keepaspectratio]{data/flow_digram_data/build/backend/03_data_models_persistence_flow.pdf}
\caption{Backend Data Model and Persistence Flow}
\label{fig:backend-data-model-persistence-flow}
\end{figure}

\ChapterSubsection{Backend Diagnosis and Advisory Pipeline}

\subsubsection{Image Validation}

Before any model runs, image validation checks incoming files for malformed headers,
unsupported formats, out-of-range dimensions, and invalid crop hints. Anything that fails is
rejected before it reaches the VLM or advisory services.

\subsubsection{VLM Inference Bridge}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.9\textwidth,height=0.4\textheight,keepaspectratio]{data/flow_digram_data/build/vlm/04_api_gateway_flow.pdf}
\caption{VLM API Fateway Flow}
\label{fig:vlm-api-gateway-flow}
\end{figure}
Built as a FastAPI service, the VLM inference gateway exposes three endpoints:
\texttt{/health}, \texttt{/ready}, and \texttt{/v1/inference}. The Express backend holds the
gateway credentials and checks readiness before forwarding any inference request
(Figures~\ref{fig:vlm-api-gateway-flow} and~\ref{fig:backend-diagnosis-pipeline-flow}).

\begin{figure}[htbp]
\centering
\includegraphics[width=0.9\textwidth,height=0.4\textheight,keepaspectratio]{data/flow_digram_data/build/backend/02_diagnosis_pipeline_flow.pdf}
\caption{Backend Diagnosis Pipeline}
\label{fig:backend-diagnosis-pipeline-flow}
\end{figure}

\subsubsection{Google AI Advisory Bridge}

The mobile app expects a specific structure from the response since it has to parse it and present it to the farmers. Therefore, we have enforced the validations and filters to ensure that the JSON response covers disease, confidence,
out-of-distribution state, symptoms, treatment, prevention, safety, source, and
alternative-diagnosis fields. 

\ChapterSubsection{Backend Security, Uploads, and Controls}

\subsubsection{Upload Handling and Cloudinary Storage}

The images have been the central part of the whole flow, such as the crop image. Therefore, we have used the standard tools to process and deal with them efficiently. We have used multer for processing the image, and with this, we also imposed strict validations. If the image could not meet the standard quality, then the request would be rejected. The images after the successful request are stored in Cloudinary with secure URLs. 

\subsubsection{Request Safety and Error Handling}

DDoS attacks are common; therefore, we have added rate-limiting features to the backends. The user can send 100 requests in 15 minutes for the general routes, 10 requests in 15 minutes for authentication, and 5 requests in an hour for OTP. In the backend, it is designed to work efficiently even if there is an error. It would send a generic error in production and a detailed error in the development response. 

\subsubsection{Authentication and Session Handling}

We have used JWT-based authentication with an access token and refresh token. When the user logs in, they will receive both the access token and refresh token. The access token is sent and used in every request. It has an expiration date, and if the access token expires, the refresh token is consumed, and new access and refresh tokens are used. For OTP, we store hashed values
with expiry and attempt counts under a unique email-purpose index, so each purpose --- email
verification, password reset, phone verification --- has its own expiration window.

\ChapterSubsection{Mobile Application Architecture}

\subsubsection{Purpose and Implementation Scope}

The mobile frontend was built with React Native and Expo \parencite{reactNativeDocs, expoDocs}. It handles onboarding, the management of crop-plots, an offline-aware queue of scans, submitting diagnoses, showing results and keeping records in bilingual form. It was designed according to the paradigm "local-first" \parencite{kleppmannLocalFirst2019}: actions from the user write to SQLite and sync with the backend. Expo Router handles navigation, TanStack Query is used to manage server-state (mutations), i18next manages the content (in both the two languages), VisionCamera records the shots from the natives' devices whilst TFLite undertakes local inference. Android package is \texttt{com. Krishivaidya. App} in scheme \texttt{krishivaidya}.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.9\textwidth,height=0.45\textheight,keepaspectratio]{data/flow_digram_data/build/mobile_app/05_system_architecture_overview.pdf}
\caption{Mobile application architecture overview showing presentation, state, native modules, and local storage boundaries.}
\label{fig:mobile-app-architecture}
\end{figure}

\subsubsection{Application Shell and Navigation Structure}

At startup, the root layout initializes fonts, the splash screen, a QueryClient with
offline-tolerant defaults, i18n, and the repository bootstrap before rendering the route tree.
Route protection redirects first-time users through onboarding before they reach the main tabs.

(Table~\ref{tab:mobile-route-groups}) shows the Five bottom tabs. The scan stack runs independently of crop management
routes, so scan results and reminders can cross-reference crop records without coupling the
two flows.

{\small
\renewcommand{\arraystretch}{1.3}
\begin{xltabular}{\textwidth}{@{} l L L @{}}
\caption{Implemented mobile route and workflow groups}\label{tab:mobile-route-groups}\\
\toprule
\textbf{Route group} & \textbf{Implemented screens} & \textbf{Primary purpose} \\
\midrule
\endfirsthead
\multicolumn{3}{l}{\footnotesize\textit{Table \thetable{} \textemdash{} continued from previous page}}\\[3pt]
\toprule
\textbf{Route group} & \textbf{Implemented screens} & \textbf{Primary purpose} \\
\midrule
\endhead
\endfoot
\bottomrule
\endlastfoot 
Onboarding & Welcome, language, permission, crop, demo & First-run setup, language selection, permission preparation, crop context \\
Main tabs & Home, Crops, Scan, History, Settings & Primary application navigation for daily use \\
Scan stack & Capture, preview, image editor, analysis, final analysis & Image acquisition, review, detection, cloud diagnosis, report viewing \\
Crop routes & Add/edit crop, crop detail, crop weather, tasks, notes & Farm plot management and agronomic record keeping \\
Settings routes & Profile, option select, notifications, accessibility, export, model precision & Preference management, data export, accessibility, model deployment controls \\
\end{xltabular}
}

\subsubsection{Provider, Repository, and Dependency Structure}

Dependencies reach the component tree through two context providers. Camera, network,
notification, haptics, voice, and location services come from \texttt{ServicesProvider}.
Scans, crops, tasks, notes, and the data catalog come from \texttt{RepositoryProvider}. During
startup, the repository bootstrap initializes SQLite, runs the V3-to-V4 migration, performs a
one-time MMKV-to-SQLite migration, constructs datasources, creates repository instances, and
starts the offline sync manager. Agricultural catalog data --- varieties, growth stages,
fertilizer recommendations, disease entries, and seasonal tips --- ships as JSON fixtures,
separate from user records in SQLite.

\subsubsection{Local Persistence and Offline-Aware Design}

Inside SQLite, the schema holds scans, offline queue entries, reminders, sync queue entries,
crop plots, tasks, notes, advisories, custom task categories, settings, achievements,
achievement statistics, and notifications. Mutable tables carry \texttt{sync\_status},
\texttt{updated\_at}, and \texttt{deleted\_at} columns so that writes complete locally before
backend synchronization runs (Table~\ref{tab:mobile-local-persistence}).

{\small
\renewcommand{\arraystretch}{1.3}
\begin{xltabular}{\textwidth}{@{} l L L @{}}
\caption{Implemented mobile local persistence groups}\label{tab:mobile-local-persistence}\\
\toprule
\textbf{Persistence group} & \textbf{Tables or stores} & \textbf{Supported manuscript interpretation} \\
\midrule
\endfirsthead
\multicolumn{3}{l}{\footnotesize\textit{Table \thetable{} \textemdash{} continued from previous page}}\\[3pt]
\toprule
\textbf{Persistence group} & \textbf{Tables or stores} & \textbf{Supported manuscript interpretation} \\
\midrule
\endhead
\endfoot
\bottomrule
\endlastfoot
Scan records & \texttt{scans}, \texttt{offline\_queue} & Local scan history, queued uploads, offline diagnosis fallback \\
Synchronization & \texttt{sync\_queue}, sync columns on mutable tables & Retryable operation queue and change tracking mechanism \\
Crop management & \texttt{crop\_plots}, \texttt{crop\_tasks}, \texttt{crop\_notes}, \texttt{crop\_advisories} & Farmer plot, task, note, and advisory records \\
Personalization & \texttt{user\_settings} & Language, theme, accessibility, onboarding, model precision, selected crops \\
Engagement and alerts & \texttt{user\_achievements}, \texttt{user\_achievement\_stats}, \texttt{notifications}, \texttt{reminders} & App engagement state, reminder scheduling, notification history \\
Catalog data & JSON fixture files through catalog repository & Read-only crop, growth-stage, fertilizer, disease, and seasonal reference data \\
\end{xltabular}
}

Monitoring connectivity, the offline sync manager also resets interrupted items on startup and
syncs every five minutes in batches of ten. When connectivity drops, processing stops. The current table-push logic marks local rows as synced, but not all entity types have
matching backend endpoints yet --- so offline sync is working infrastructure with partial
backend coverage, not yet a fully bidirectional system
(Figure~\ref{fig:mobile-offline-sync-data-flow}).

\begin{figure}[htbp]
\centering
\includegraphics[width=0.9\textwidth,height=0.4\textheight,keepaspectratio]{data/flow_digram_data/build/mobile_app/03_offline_sync_data_flow.pdf}
\caption{Mobile offline synchronization data flow showing local writes, queue management, connectivity checks, and retry behavior.}
\label{fig:mobile-offline-sync-data-flow}
\end{figure}
\subsubsection{Scan Workflow and Diagnosis Flow}Capture or gallery selection starts the scan workflow, followed by preview, optional editing,diagnosis submission, result display, and local history update(Figure~\ref{fig:mobile-scan-pipeline-flow}). State transitions, network branching, timeouthandling, and retry logic are covered in the Algorithm Details section.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.9\textwidth,height=0.6\textheight,keepaspectratio]{data/flow_digram_data/build/mobile_app/02_scan_pipeline_flow.pdf}
\caption{Mobile scan pipeline flow showing capture, preview, editing, diagnosis submission, and retry-aware request handling.}
\label{fig:mobile-scan-pipeline-flow}
\end{figure}

\subsubsection{On-Device Model Asset Support}

A singleton TFLite service manages three precision variants:
\texttt{krishi\_bouncer\_int8.tflite}, \texttt{krishi\_bouncer\_fp16.tflite}, and
\texttt{krishi\_bouncer\_fp32.tflite}, with runtime precision selection and delegate fallback
for Android inference (Figure~\ref{fig:mobile-ondevice-ml-inference-flow}).

\begin{figure}[htbp]
\centering
\includegraphics[width=0.9\textwidth,height=0.45\textheight,keepaspectratio]{data/flow_digram_data/build/mobile_app/04_ondevice_ml_inference_flow.pdf}
\caption{Mobile on-device ML inference flow showing model selection, load fallback, and runtime precision switching.}
\label{fig:mobile-ondevice-ml-inference-flow}
\end{figure}

\subsubsection{Localization, Accessibility, and Farmer-Facing Output}

For language, the app defaults to Nepali, resolved first from the SQLite preference, then from
the device locale, then falling back to Nepali. Result screens support treatment timelines,
organic-alternative toggling, reminder creation, share-card generation, PDF export, voice
output, and feedback submission.

\ChapterSubsection{Integrated Diagnosis Workflow}

All four subsystems cooperate here. The mobile scan pipeline captures and routes the image, the
backend validates it and calls the VLM gateway, the gateway returns a prediction, and the
advisory client produces a structured response that the mobile app presents to the farmer.
Figure~\ref{fig:integration-workflow} shows the combined flow.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.9\textwidth,height=0.4\textheight,keepaspectratio]{data/flow_digram_data/build/00_all_flows.pdf}
\caption{Combined flow-diagram overview covering the subsystem workflows used in the manuscript.}
\label{fig:integration-workflow}
\end{figure}

Deployment splits runtime responsibilities across five nodes: the Android device, the Express
backend, MongoDB and Cloudinary storage, the Google AI advisory service, and the FastAPI VLM
gateway. Model-service credentials stay behind the backend boundary; YOLO TFLite variants ship
inside the mobile APK for on-device plant-part detection. The step-by-step online diagnosis
procedure is listed in Table~\ref{tab:integration-control-flow} under Algorithm Details.

\ChapterSection{Algorithm Details}

What follows are the procedures that run across the components, data stores, and boundaries
described above. Configuration values appear where they shape the procedure; measured outcomes
are reported in Chapter~5.

\ChapterSubsection{YOLO Training, Validation, and Deployment Procedure}

\subsubsection{Training Setup}

For training, we begin from the curated plant-part dataset. Image records and bounding-box
annotations are exported to YOLO format, then trained on \texttt{yolov8n.pt} with 640-pixel
inputs for 100 epochs. Mosaic augmentation runs at probability 1.0 and shuts off during the
final 10 epochs to reduce late-stage noise; mixup and copy-paste are disabled
(Table~\ref{tab:yolo-training-configuration}).

{\small
\renewcommand{\arraystretch}{1.3}
\begin{xltabular}{\textwidth}{@{}ll@{}}
\caption{YOLO training configuration used for the Krishi Vaidya detector}\label{tab:yolo-training-configuration}\\
\toprule
\textbf{Parameter} & \textbf{Value} \\
\midrule
\endfirsthead
\multicolumn{2}{l}{\footnotesize\textit{Table \thetable{} \textemdash{} continued from previous page}}\\[3pt]
\toprule
\textbf{Parameter} & \textbf{Value} \\
\midrule
\endhead
\endfoot
\bottomrule
\endlastfoot
Task & Object detection \\
Base model & \texttt{yolov8n.pt} \\
Epochs & 100 \\
Image size & 640 \\
Validation & Enabled \\
Optimizer & Auto-selected \\
Initial learning rate & 0.01 \\
Final learning-rate factor & 0.01 \\
Momentum & 0.937 \\
Weight decay & 0.0005 \\
Warmup epochs & 3.0 \\
Box loss gain & 7.5 \\
Classification loss gain & 0.5 \\
DFL gain & 1.5 \\
Mosaic augmentation & 1.0 \\
Horizontal flip probability & 0.5 \\
Close mosaic & Last 10 epochs \\
\end{xltabular}
}

\subsubsection{Validation and Threshold Selection}

Our validation pipeline reports precision, recall, F1-score, mAP@50, and mAP@50-95. mAP@50
evaluates detections at IoU 0.50; mAP@50-95 applies the COCO range from 0.50 to 0.95. A
confidence sweep from 0.05 to 0.95 selects the deployment threshold: lower values preserve
more detections but admit weaker predictions; higher values suppress weak detections but miss
more objects. The operating point is a precision--recall tradeoff, not a single-metric maximum.

\subsubsection{Export and Mobile Post-Processing}

The detector exports to FP32, FP16, and INT8 TFLite variants, keeping the same class taxonomy.
On device, the selected variant takes a 640-pixel RGB input. For each detection row, the
post-processor picks the highest class score, multiplies it by objectness to get confidence,
drops candidates below the confidence threshold, and runs non-maximum suppression at the
configured IoU threshold. Surviving boxes are returned with class id, class name, confidence,
and bounding-box coordinates.

\ChapterSubsection{VLM Fine-Tuning and Prediction-Parsing Procedure}

\subsubsection{Adapter Fine-Tuning}

We converted curated crop-disease records into Qwen-compatible prompt-response examples and
fine-tuned \texttt{Qwen/Qwen2.5-VL-3B-Instruct} with LoRA adapters
\parencite{hu2021lora}. Adapters target the attention and feed-forward projection layers:
\texttt{q\_proj}, \texttt{k\_proj}, \texttt{v\_proj}, \texttt{o\_proj},
\texttt{gate\_proj}, \texttt{up\_proj}, and \texttt{down\_proj}. Modules matching
\texttt{visual}, \texttt{vision}, or \texttt{merger} are excluded so that only language-side
weights are adapted.

The model loads under 4-bit NF4 quantization with double quantization, bfloat16 compute dtype,
flash attention, and automatic device mapping. Three epochs reflect the available GPU budget,
not a deliberate early stop --- training consumed 12.56~GB of the 22.03~GB available on the
L4. The Liger kernel is enabled; \texttt{torch.compile} is disabled
(Table~\ref{tab:vlm-finetuning-configuration}).

{\small
\renewcommand{\arraystretch}{1.3}
\begin{xltabular}{\textwidth}{@{}ll@{}}
\caption{VLM fine-tuning configuration}\label{tab:vlm-finetuning-configuration}\\
\toprule
\textbf{Parameter} & \textbf{Value} \\
\midrule
\endfirsthead
\multicolumn{2}{l}{\footnotesize\textit{Table \thetable{} \textemdash{} continued from previous page}}\\[3pt]
\toprule
\textbf{Parameter} & \textbf{Value} \\
\midrule
\endhead
\endfoot
\bottomrule
\endlastfoot
Base model & \texttt{Qwen/Qwen2.5-VL-3B-Instruct} \\
Training method & LoRA adapter fine-tuning \\
Quantization & 4-bit NF4 with double quantization \\
Compute dtype & bfloat16 \\
LoRA rank / alpha & 16 / 32 \\
LoRA dropout & 0.05 \\
Epochs & 3 \\
Learning rate & \(2.0 \times 10^{-5}\) \\
Train batch / accumulation & 4 / 4 \\
Effective batch & 16 samples \\
Optimizer & \texttt{paged\_adamw\_8bit} \\
Scheduler & Cosine \\
Max sequence length & 4096 \\
Attention backend & \texttt{flash\_attention\_2} \\
GPU & NVIDIA L4 \\
\end{xltabular}
}

\subsubsection{Prediction Parsing}

During inference, the gateway receives a crop image and a diagnosis prompt. The generated text
gets parsed as a complete JSON object first. If that fails, the parser tries extracting the
first embedded JSON object. A \texttt{canonical\_label} field, if present, becomes the
predicted label. Without one, we tag the prediction \texttt{\_\_parse\_error\_\_}. This prevents free-form text
from passing as a valid diagnosis and makes parseability part of model evaluation.

\ChapterSubsection{Backend Diagnosis-Orchestration Procedure}

\subsubsection{Request Validation and VLM Call}

\texttt{POST /api/v1/diagnose} accepts a multipart request with the image in the
\texttt{image} field and an optional crop-name hint. Validation checks file existence, upload
size, image headers, MIME consistency, dimensions, and format. We reject images outside
\(64 \times 64\) to \(4096 \times 4096\) pixels and cap the crop-name hint at
80 characters. We accept JPEG, PNG, and WEBP.

Once validated, the diagnosis service checks VLM gateway readiness at \texttt{/ready}. If the
gateway is unconfigured, unreachable, timed out, or not ready, the backend raises a
service-unavailable error rather than returning a partial result. Otherwise it sends an
authorized \texttt{/v1/inference} request with base64 image data, media type, request id,
deterministic generation settings, a 320-token output limit, and a crop-disease prompt.
Auth uses either a static bearer token or a cached RS256 JWT.

\subsubsection{Advisory Generation and Response Assembly}

Parsing the VLM prediction uses the JSON rules described above. From there, the backend
forwards the parsed context and validated image to the Google AI advisory client. The advisory
response must pass schema validation before acceptance. We clamp confidence to \([0, 1]\),
cap alternatives at three, and assign a confidence tier: \(\geq 0.85\) maps to high, 0.60--0.85
to medium, lower non-OOD values to low, and out-of-distribution or below 0.30 to OOD. The final response that would be sent to the mobile app is then the user made the request for diagnosis with the crop image, containing request metadata, disease fields, symptoms, advisory content, processing time, and model version.

\ChapterSubsection{ Online Diagnosis Procedure}

Table~\ref{tab:integration-control-flow} shows the complete request lifecycle, from when the user made the diagnosis request with the crop image to when the farmer gets the report about the diagnosis.

{\small
\renewcommand{\arraystretch}{1.3}
\begin{xltabular}{\textwidth}{@{} l L L L @{}}
\caption{ Online Diagnosis Control Flow}\label{tab:integration-control-flow}\\
\toprule
\textbf{Step} & \textbf{Source component} & \textbf{Implementation evidence} & \textbf{Output} \\
\midrule
\endfirsthead
\multicolumn{4}{l}{\footnotesize\textit{Table \thetable{} \textemdash{} continued from previous page}}\\[3pt]
\toprule
\textbf{Step} & \textbf{Source component} & \textbf{Implementation evidence} & \textbf{Output} \\
\midrule
\endhead
\endfoot
\bottomrule
\endlastfoot
1 & Mobile scan workflow & Capture, preview, editor, analysis view models & Image URI and optional crop context \\
2 & Mobile diagnosis hook & Network-aware mutation with online, slow, offline branches & Cloud request, cached result, or queued scan \\
3 & Mobile API & Multipart \texttt{/api/v1/diagnose} request & Send diagnosis request \\
4 & Route of Backend diagnosis  & image upload middleware and diagnosis controller & Correct Diagnosis Input \\
5 & Backend validation & image size, dimension, and crop-name checks & Correct Diagnosis Input \\
6 & VLM client & \texttt{/ready} and then request to \texttt{/v1/inference} with prompt and auth token & VLM response data \\
7 & Backend parser & Parsing the response & Structured diagnosis json data \\
8 & Diagnosis client & Request to the AI for the structured json response & Correct diagnosis and advisory data \\
9 & Response of Backend  & time, confidence level, model versions & Application-facing JSON response \\
10 & Mobile result & Result page, history, reminders, feedbacks, data export & Farmer-facing report and local record \\
\end{xltabular}
}

\ChapterSubsection{Mobile Offline and Result-Handling Procedure}

\subsubsection{Scan State and Network Branching}

After image capture (or gallery selection), preview, optional editing, and quality review, the
analysis view model enters one of seven phases: \texttt{ready}, \texttt{scanning},
\texttt{reviewed}, \texttt{sending}, \texttt{success}, \texttt{failed}, or \texttt{queued}.
Network state determines the next step: online sends a cloud request; slow races the request
against a five-second timeout and falls back on failure; offline skips the backend and queues
the scan.

We write queued scans to SQLite scan history and the unified sync queue as
\texttt{scan\_diagnosis} entries. If a cached advisory for the crop context is available, it
fills in while the scan waits for cloud diagnosis. The API client applies a 60-second timeout
and retries transient failures (408, 429, 500, 502, 503, 504) with exponential backoff.
\subsubsection{Farmer-Facing Result Mapping}Completed responses get mapped into bilingual disease details, confidence display, treatmenttimelines, prevention advice, safety warnings, reminders, feedback, PDF export, and sharecards by the result view. Based on the selected language the label or text would appear on the mobile app. In the treatment step it would also show the organic alternatives. The completed scan gets saved for later access from crop or history screens.