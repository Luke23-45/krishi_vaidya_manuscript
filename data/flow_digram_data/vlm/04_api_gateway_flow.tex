% VLM — API Gateway Flow
% Source: manuscript/flow_diagram/vlm/04_api_gateway_flow.md
% Preamble requirements:
%   \usepackage{tikz}
%   \usetikzlibrary{arrows.meta,positioning,calc,fit,backgrounds,shadows.blur}
\begin{figure}[H]
\centering
\hyphenpenalty=10000
\exhyphenpenalty=10000
\resizebox{0.98\textwidth}{!}{%
\begin{tikzpicture}[
  font=\sffamily,
  flow/.style={-{Latex[length=2.8mm,width=2mm]}, line width=0.8pt,
               draw=pri!70, rounded corners=6pt},
  flowret/.style={-{Latex[length=2.2mm,width=1.5mm]}, line width=0.5pt,
                  draw=muted!45, densely dashed},
  card/.style={rounded corners=10pt, draw=ink!10, line width=0.55pt,
               fill=white, align=left, inner sep=8pt,
               font=\sffamily\scriptsize, text=ink,
               blur shadow={shadow blur steps=5, shadow xshift=0.6pt,
               shadow yshift=-1pt, shadow opacity=13}},
  groupbox/.style={rounded corners=11pt, draw=#1!22, line width=0.4pt,
                   fill=#1!3},
  grouplbl/.style={font=\sffamily\bfseries\tiny, text=#1!70,
                   fill=white, inner xsep=3pt, inner ysep=1.5pt,
                   rounded corners=2pt},
  layercard/.style={card, align=center, inner sep=6pt,
                    minimum width=8cm, minimum height=1cm,
                    fill=#1!4, draw=#1!25},
  clientcard/.style={card, align=center, inner sep=6pt,
                     minimum width=2.5cm, fill=muted!5, draw=muted!25},
  arrlbl/.style={font=\sffamily\tiny\itshape, text=muted,
                 fill=white, inner sep=1.5pt, rounded corners=1pt},
  note/.style={rounded corners=7pt, draw=ink!10, fill=ink!2,
               inner sep=8pt, align=left, font=\sffamily\tiny,
               text=ink!78, text width=14.2cm}
]

\definecolor{pri}{HTML}{4338CA}   % Indigo
\definecolor{sec}{HTML}{0D9488}   % Teal
\definecolor{ter}{HTML}{D97706}   % Amber
\definecolor{quat}{HTML}{9333EA}  % Purple
\definecolor{bg}{HTML}{F8FAFC}
\definecolor{ink}{HTML}{0F172A}
\definecolor{muted}{HTML}{64748B}


% ━━ LAYER 1: CLIENTS (Fan-in) ━━
\node[clientcard] (client1) at (-3.5,-2.2) {Node.js Backend\\[-1pt]{\tiny Primary client}};
\node[clientcard] (client2) at ( 0.0,-2.2) {Python SDK\\[-1pt]{\tiny Evaluation}};
\node[clientcard] (client3) at ( 3.5,-2.2) {cURL / Postman\\[-1pt]{\tiny Debugging}};

% ━━ LAYER 2: FASTAPI GATEWAY ━━
\node[layercard=sec] (gateway) at (0,-4.5) {
  \textbf{FastAPI Gateway (Port 8000)}\\[2pt]
    \textcolor{ink!72}{\tiny $\bullet$ Rate Limiting \& Auth Verification}\\[-1pt]
  \textcolor{ink!72}{\tiny $\bullet$ Payload Validation (Pydantic)}\\[-1pt]
  \textcolor{ink!72}{{\tiny $\bullet$ Error formatting \& timeout handling}}};
\draw[flow] (client1.south) -- ++(0,-0.4) -| (gateway.110);
\draw[flow] (client2.south) -- (gateway.north);
\draw[flow] (client3.south) -- ++(0,-0.4) -| (gateway.70);

% ━━ LAYER 3: VLLM SERVER ━━
\node[layercard=pri] (vllm) at (0,-7.0) {
  \textbf{vLLM OpenAI-Compatible Server (Port 8080)}\\[2pt]
    \textcolor{ink!72}{\tiny $\bullet$ API: /v1/chat/completions}\\[-1pt]
  \textcolor{ink!72}{\tiny $\bullet$ Continuous Batching \& PagedAttention}\\[-1pt]
  \textcolor{ink!72}{{\tiny $\bullet$ Hardware: GPU 0 (CUDA)}}};
\draw[flow] (gateway) -- (vllm) node[arrlbl, midway, right] {Reverse Proxy};

% ━━ RETURNS ━━
\draw[flowret] (vllm.170) -- (gateway.190) node[arrlbl, midway, left] {JSON};
\draw[flowret] (gateway.175) -- ++(-1.2,0) |- (client1.190);

% ━━ NOTE ━━
\node[note] at (0,-9.2) {
  \textbf{Separation}\;\textcolor{muted}{\textemdash}\;
  The FastAPI gateway acts as a thorough front door. The internal vLLM server is never
  exposed directly to clients; this prevents malformed requests from crashing the GPU
  worker and provides a layer for metrics and API key validation.};

% ━━ BACKGROUND ━━
\begin{scope}[on background layer]
  \node[rounded corners=14pt, fill=bg!45, draw=ink!4,
        line width=0.35pt, inner sep=14pt,
        fit=(client1)(client3)(gateway)(vllm)] {};

  \node[groupbox=sec, fit=(gateway)(vllm),
        inner xsep=10pt, inner ysep=8pt] (gServ) {};
  \node[grouplbl=sec, anchor=north west]
        at ([xshift=2pt]gServ.north west) {Inference Infrastructure};
\end{scope}

\end{tikzpicture}%
}
\caption{VLM API Gateway Flow}
\label{fig:vlm-04-api-gateway-flow}
\end{figure}
